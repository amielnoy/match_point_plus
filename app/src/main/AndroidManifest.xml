<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matchpointplus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MatchPointPlus"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".views.MatchesActivity" />
        <activity android:name=".views.ChatActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".views.ProfileActivity" />
        <activity android:name=".views.SignUpActivity" />
        <activity android:name=".views.SearchMatchesActivity" />
        <activity android:name=".views.AdminActivity" />
        <activity android:name=".views.AddCandidateActivity" />
        <activity android:name=".views.MessagesListActivity" />
        
    </application>

</manifest>
